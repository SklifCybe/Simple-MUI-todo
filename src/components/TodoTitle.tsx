import React, { FC } from 'react';
import { Typography } from '@mui/material';

export const TodoTitle: FC = () => {
  return (
    <Typography component="h1" variant="h5">
      Simple TODO
    </Typography>
  );
};
